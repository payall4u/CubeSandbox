# S5.5d.2 durable create、持久化与 Host cgroup 快路径

## 状态与目标

状态：`S5.5d.2a_PASS`。同一 reviewer 已终审，P0/P1/P2 均为 0。基线使用 S5.5d.1 已终审的
`overlap-v2` 首次正式 50 串行与 5×10
并发原始 trace，不重跑或挑选样本。本阶段保留原子持久化、目录 fsync、精确 identity/readback
和失败恢复，关闭以下门禁：

- `Shim create begin→start vm` 串行/并发 P95≤140/150ms；
- `CRI receive→start vm` 串行/并发 P95≤220/300ms；
- 每轮 10 Pod absolute VMM start 展开≤250ms；
- 正常路径 `systemctl show=0`，全部 atomic persistence、kill/restart 与 exact-zero 回归通过。

## 冻结基线账本

下表只统计同一 Pod 的 Shim `CreateSandbox` handler 开始至 `start-vm-begin` 之前的事件。
原子写耗时是 21 个顺序提交的直接 wall time 之和；它已经包含各自的 directory fsync，不能再与
内层 fsync 相加。P95 使用 nearest-rank 第 48 个样本。

| 指标 | 串行 50 P95 | 5×10 并发 P95 | 当前门禁 |
|---|---:|---:|---:|
| Shim create begin→start vm | 231.028ms | 438.794ms | ≤140/150ms，未通过 |
| CRI receive→start vm | 303.879ms | 566.857ms | ≤220/300ms，未通过 |
| systemd 固定 stability gate | 80.382ms | 81.441ms | 应删除固定等待 |
| systemd placement ready | 27.422ms | 20.162ms | 保留事件/内核可见门禁 |
| start-vm 前全部 atomic JSON，21 次/Pod | 91.812ms | 226.921ms | 降低提交数 |
| host-cgroup-owner，12 次/Pod | 52.656ms | 133.587ms | controller journal 合并 |
| record，4 次/Pod | 19.690ms | 45.676ms | 保留 owner/commit 语义 |
| runtime-resource-owner，5 次/Pod | 22.856ms | 57.499ms | 合并相邻状态提交 |
| RuntimeResource 总耗时 | 37.006ms | 118.738ms | 删除冗余 WAL 阶段 |
| RuntimeResource RPC | 27.401ms | 96.570ms | 继续拆分 Host/Provider |

5 个并发 round 的 absolute VMM start 展开为
116.725/122.376/82.063/128.685/91.600ms，P95=128.685ms，已经通过≤250ms；不得通过限并发
或删样本换取其他分位数。

## 子阶段

### S5.5d.2a：低风险等待与相邻 WAL 合并

- Cubelet 新建记录只提交完整 `INTENT` 和最终 `PREPARED`；`SHARED_ROOT` 仍可读取，用于旧记录
  恢复。INTENT 已包含 shared root、network target 与 cleanup identity，崩溃后可幂等重放。
- systemd `StartTransientUnit` 后只等待 cgroup leaf、parent identity 和目标 PID membership
  同时可见；取得 leaf inode 后立即精确复读，不再固定等待 4×20ms。后续 ALLOCATED 与
  CONTAINERD_COMMITTED 仍各自 durable commit，并再次验证 membership。
- TAP 已取得后，用一次 runtime owner 提交同时记录 `TAP=ALLOCATED` 和 `VM=INTENT`；此前的
  `TAP=INTENT` 已包含精确 provider cleanup identity，崩溃窗口仍可回收。

验收：相关 unit/race 与既有 failpoint 全通过；单 Pod 注入窗口能恢复/回滚；同一 reviewer
`PASS` 后才进入 S5.5d.2b。

#### S5.5d.2a 实施与证据

实现候选相对 `587ba998`：

- Cubelet 新请求不再提交历史 `SHARED_ROOT` 阶段，持久化序列为 `INTENT → PREPARED`；旧
  `SHARED_ROOT` 记录仍可恢复。`adapter-resume` 实测 `persist_shared_us=0`。
- CubeShim 删除固定 4×20ms stability sleep，改成 readiness 联合观察后立即复读 parent/leaf/PID
  identity；日志明确为 `samples=2 strategy=event-exact-readback`。
- TAP handoff 后用单次原子 owner commit 同时发布 `TAP=ALLOCATED / VM=INTENT`，并覆盖 temp、
  file-fsync、rename、parent-fsync 四个失败边界。
- 崩溃实测发现 `SandBox::init` 已建 VM 目录、但 worker 尚未启动时，旧 watchdog 只能释放外部
  RuntimeResource，不能执行 `delete_shim` 的本地目录清理。修复后由 runtime cleanup queue consumer
  在外部 release 成功后幂等调用本地 sandbox cleanup；sandbox ID 在任何 path join 前校验，job
  只在两类清理均成功后 ack。
- reviewer 发现旧校验接受精确 `.`，新增清理若消费损坏记录会把 VM 根目录当作 sandbox 目录。
  最终候选把 Host 与 cleanup 校验统一为 1～128 字节、仅 ASCII `[A-Za-z0-9_.-]`，并显式拒绝
  `.`、`..`、嵌入式 `..` 和所有其他字符；root-alias 测试证明在任何删除前 fail closed。

本地门禁：

- `cargo fmt --check --all`：PASS；
- `cargo check --tests --locked`：PASS（仅项目既有 warning）；
- `go test -race ./plugins/cube/runtime_resource`：PASS；
- `git diff --check`：PASS。

W3 `ins-et16iojk` 精确源文件构建：

- source patch SHA-256：`c8a3453863b2220c74479ed8871916d1fac6329be26115902895735fff577e69`；
- Rust overlay SHA-256：`cd160d6568ffa95ab004f2a967b3a01e04e8c44b7609e8d04fd0d5dd2ff07e84`；
- root-alias 精确测试：`1 passed, 0 failed, 298 filtered out`；完整 Host test filter：
  `82 passed, 0 failed, 217 filtered out`；release build PASS；
- 构建 invocation：`inv-b8dpx40cud`；精确审计：`inv-98dq3g0q8d`；
- CubeShim SHA-256：`32d3a6eb28b66ae9b0a52b275a69e08a0a7c2707a8d56c8d414bae07e781474d`；
- cube-vmm-worker SHA-256：`cd401d898d918a2b93d0b63c55dca4a58893998d6e822042d222670de91a5da1`。

W2 `ins-4dyul5ag` 普通路径：

- 部署 invocation `inv-68dq5cg8qp`，备份
  `/opt/cubesandbox-s0-multinode-runtime-2269a3b3/backups/s55d2a-v6-w2-20260908T080545Z`；W2 保持
  cordoned；
- Pod UID `9624d87f-385c-4b1b-9a48-a33368ef8f46`，sandbox
  `9c75072a6fa89c981954c129826b31fa3595c829046415e61b029881e6bafa0f`；
- `RuntimeClass=cube`、`runtimeHandler=cube`、新 CubeShim/worker 精确哈希、单一 Shim/worker、同一
  精确 Pod cgroup、RuntimeResource `ALLOCATED`、adapter `PREPARED` 全部通过；审计
  `inv-a8dq6e0ase`；
- 单样本 `Shim Create begin→start VM=134.482ms`，固定 stability gate 为 12µs；start VM 前
  runtime owner/host owner/record 原子写为 4/12/4；RuntimeResource resume 9.439ms，且
  `persist_shared_us=0`；性能审计 `inv-68dq6sgbwi`；第一次 `inv-b8dq690fpx` 与身份审计并发启动，
  在输入文件生成前退出，不计作实现失败或验收样本；
- 普通删除后独立 13 项 exact-zero：PASS，`inv-88dq7p0ft4`，原始文件
  `exact-zero-v6/w2-normal.txt`。

W2 最终候选同位点崩溃回归：

- Pod UID `767f81b5-277b-482b-8649-89979a4415e5`；在
  `TAP=ALLOCATED / VM=INTENT / vmm_worker=null` 时核对最终 CubeShim 哈希后 SIGKILL PID 405213；
  sandbox `d752caae4211e121098918a865124f1bf894aaf5b8f4a9437d3f289c66bf7e6f`；
- failpoint 启用 `inv-68dq88g07v`，释放非目标窗口 `inv-v8dqan05ij`，kill/restart
  `inv-v8dq93gisb`；failpoint drop-in 随即移出并恢复 containerd/CRI；
- 删除 namespace 后无需人工处理，sandboxes/workers/shims/vms/lifecycle/host jobs/runtime jobs/
  adapter/reapers/shared/TAP/mounts/active leases 十三项全为 0，`inv-38dqbpgtrx`，原始文件
  `exact-zero-v6/w2-inject.txt`；
- 最终 W2 服务、制品、failpoint 环境和两份独立 exact-zero 审计 PASS：`inv-08dqceg9q4`；控制面
  确认 W2 Ready+cordoned、W1 Ready、测试 namespace 清空：`inv-v8dqchgeqi`。未向 W1 执行 TAT。

历史 v3 注入残留的唯一空 VM 目录不计作任何最终候选自动恢复。部署中间候选 v4 前，先由独立脚本核对其原始
record、inactive release tombstone、CRI、进程、live WAL/job、adapter、reaper、mount 和目录内容；
确认无活引用且目录为空后用精确 `rmdir` 退役，invocation `inv-68dpa40cfv`，全部审计材料保留在
历史 v4 证据包。最终 v6 的正常与注入两条链从 clean preflight 开始，均未做人工清理，且分别保留
独立 exact-zero 原始文件。

COS 与反向下载证据：

- W2：`kubernetes-runtime/s5.5d.2a/evidence/s55d2a-v6-w2-bd8f6325.tar.gz`，SHA-256
  `bd8f6325344f0f70fb72f63587446534c6bf4602b40c78f6e6a845c7a5d8c9cf`；
- W3：`kubernetes-runtime/s5.5d.2a/evidence/s55d2a-v6-w3-c61b7917.tar.gz`，SHA-256
  `c61b7917c9f7e3ecec92bf975818e15c088253a64e757688660c46d385730b8a`；
- 控制面：`kubernetes-runtime/s5.5d.2a/evidence/s55d2a-v6-control-a09632b0.tar.gz`，SHA-256
  `a09632b079e395898bdfe2ef00b692fed875ce1c94ffca906beefefecdd1ee84`；
- 三包已从 COS 反向下载并校验同一 SHA-256，review 目录：
  `/tmp/s55d2a-v6-review.IBrxRq`。

同一 reviewer 独立核对当前五个代码文件 diff 与 W3 source patch/overlay、W3 原始测试和 release
日志、W2 最终二进制与 Pod cgroup identity、普通/注入两份独立 exact-zero、TAT 任务状态、控制面
终态和本地门禁，最终明确返回 `PASS S5.5d.2a`，P0=0、P1=0、P2=0。

### S5.5d.2b：Host controller 批事务

- 新事务在任何 controller 外部写之前，一次提交四个 controller 的 canonical old/target 和
  owner identity（`PREPARED`）。
- 对四个 controller 幂等写 target 并逐项精确回读；全部成功后一次提交
  `CONTROLLERS_COMMITTED`。不得每步写前/写后重复 fsync journal。
- 恢复看到 PREPARED 时，每个实际值必须是 old 或 target；合法时按 owner 状态续做或回滚，
  出现第三值则进入 durable DEGRADED。旧 schema 的逐步 journal 继续可恢复。

验收：提交数量由 controller 路径 10 次降为 2 次；每个 write/readback failpoint、Shim/worker
kill 和 restart 都收敛；同一 reviewer `PASS` 后才进入 S5.5d.2c。

### S5.5d.2c：云上正式门禁与阶段关闭

- 固定 digest、`imagePullPolicy: Never`，首次完整 50 串行与 5×10 并发，成功率 100%；
- 报告提交/fsync 次数、三个冻结性能门禁和完整 Pod/Sandbox/Shim/worker 唯一绑定；
- 回归 DNS、ClusterIP、跨节点 PodIP、NetworkPolicy、创建中取消、containerd/Cubelet restart、
  worker/Shim kill 与双节点 exact-zero；
- 原始证据反向下载复验并由同一 reviewer 给出 `PASS S5.5d.2` 后，才能进入 S5.5e。

## 当前未完成

- S5.5d.2b controller 批事务与完整故障矩阵；
- S5.5d.2c 云端 50+50、功能/故障回归、证据包与 reviewer。
