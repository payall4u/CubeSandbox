# Kubernetes RuntimeClass PoC 未决问题

| ID | 问题 | 当前假设 | Owner | 最迟 Stage | 状态 | 所需证据/最终决定 |
|---|---|---|---|---|---|---|
| K8S-OQ-001 | containerd 2.3 Sandbox API 的实际 CRI 调用顺序和配置字段是什么？ | 使用 `sandboxer = "shim"`，CubeShim 同时承载 Sandbox/Task | Codex | S0 | DECIDED | handler 使用 `runtime_type/runtime_path/sandboxer = "shim"`；bootstrap v3 返回 ttrpc endpoint，Sandbox/Task 同 PID；CNI ADD 在 Create 前，Stop 后 CNI DEL，再 Shutdown/delete。正常与四类异常的 10 类残留均为 0，证据 `inv-68246d0jt1` 和 `evidence/s0.1/` |
| K8S-OQ-002 | VM 启动后新增 bind mount 是否能被固定 virtiofs shared root 稳定看到？ | 每 Pod 一个固定 shared root 可支持动态容器/卷 | Codex | S0 | DECIDED | `cache=never/read_only=true/announce_submounts=false` 下 bind、rename、只读立即可见；普通 live unmount 可能因 virtiofsd inode 引用 `EBUSY`，必须 Guest-first 解除消费、Host `MNT_DETACH`、generation 路径不复用。实测 Guest 对非根目录仍有 86400 秒 entry/attribute timeout，因此 Pod VM 存活期间还必须保留 managed `rootfs` 父目录 inode，只替换/删除内部 generation；连续两个真实 Task 已通过。证据 `inv-9827ikgt4f`、`inv-9837xq0wnq` |
| K8S-OQ-003 | PoC 网络首先采用哪种数据面？ | 先用 Cilium，保留 VPC-CNI/Global Router adapter | Codex | S0 | DECIDED | Cilium 1.20.0 tcfilter 已实测 Pod IP/MAC/MTU、Cluster DNS、Service ClusterIP、跨节点 PodIP 和 egress NetworkPolicy；成功/失败资源残留均为 0。VPC-CNI/Global Router 作为后续 adapter 兼容项；证据 `inv-a82g9g0x1f` 和 `evidence/s0.3/` |
| K8S-OQ-004 | Kubernetes 路径能否完全使用标准 `CreateTaskRequest.rootfs`？ | 可以，legacy 私有注解只服务原链路 | Codex | S0 | DECIDED | 可以。CubeShim 解析标准 overlay active snapshot，按 active upper + image lowers 原序导出并内部适配当前 Agent；CRI/containerd 不提供私有 rootfs 输入。S1.2 已在 containerd 2.3.4 Task API v3 下连续运行自然退出 23 和 SIGKILL 137 的真实 Task；删除后 container/Task/Sandbox/Task 新增 snapshot/mount 残留均为 0，验收前后的既有 snapshot 集合一致。Guest 临时 upper 不回写 containerd 的限制在 S3 处理；证据 `inv-9827ikgt4f`、`inv-9837xq0wnq` |
| K8S-OQ-005 | Cubelet 节点 RPC 的最小边界是什么？ | 只做 VM/网络资产准备、释放和对账 | Codex | S0 | DECIDED | `runtime.v1.RuntimeResource` 仅含 capability、Prepare、Release、Inspect 和 report-only Reconcile；host containerd 独占 CRI/image/snapshot/Sandbox/Task，Cubelet service 禁止回入内嵌 containerd；TAP FD 经 cubetap `SCM_RIGHTS` 交付。证据 `40f4389a` 和 `evidence/s0.4/` |
| K8S-OQ-006 | PoC VM 规格如何确定？ | 默认 1 vCPU/512 MiB，显式配置最小允许 256 MiB；生产再做资源聚合 | Codex | S5.5a | DECIDED | 早期 S1.4 的 Job、Deployment、取消和 100 Pod 矩阵使用 1 vCPU/256 MiB；Node E2E 收口后为容纳 200Mi workload 和 rootfs page cache，将无显式资源 Pod 默认值调整为 512MiB，显式 `cube.vmmres` 仍允许 256MiB。S5.5a 必须以最终 512MiB 默认值重新建立性能与密度基线 |
| K8S-OQ-007 | ConfigMap/Secret 动态更新能否透过 virtiofs 保持语义？ | 使用独立 `cubeVolumes cache=none` share 传播 kubelet atomic-writer 更新；subPath 保持创建时 inode | Codex | S3.1c | DECIDED | S3.1c `20881f71` / `inv-a83th50gfh` 验证 ConfigMap、Secret、projected、downwardAPI 的启动值、mode 和动态更新：runc 1 秒、Cube 0 秒读到完整 `v2`，Host/Guest `..data` target 与 inode 均变化；writer/peer、Sandbox、shim、VM、Task generation 和 mount ID 稳定。两种 runtime 的 subPath 均保持 `v1` 与原 inode；删除后恢复全量基线，证据见 `evidence/s3.1/` |
| K8S-OQ-008 | Snapshot/Restore 的架构约束、CRD、artifact 与恢复语义如何定版？ | 先完成 worker 普通启动路径与 Kubernetes Node E2E；S5.4 只保留版本化 IPC、稳定设备槽位和显式 FD handoff 扩展缝，S6.1 再冻结 `RuntimeTemplate`/`PodSnapshot`/Pause/Resume 切点、owner、设备重绑定和 compatibility key | Codex/用户 | S6.1 | DEFERRED | 用户决定先拆 `cube-vmm-worker` 并在新快路径完成 E2E，再讨论模板与暂停恢复；仍需确认 PodSnapshot 是否保留进程状态、writable layer/emptyDir、Secret/ConfigMap 重投射、PVC/CSI 协调、节点本地/COS 分发、保留及失败状态，未确认前不冻结不可逆 artifact 格式 |
| K8S-OQ-009 | Sandbox API 的 `Spec` 应返回什么 protobuf `Any.type_url`？ | 返回 OCI Spec JSON 的 containerd 标准 Any | Codex | S1.4 | DECIDED | `type_url=types.containerd.io/opencontainers/runtime-spec/1/Spec`，`value` 为 OCI JSON；`ctr` 解码、CRI inspect 和 containerd warning=0，证据 `inv-983d0j0cbd`、`evidence/s1.4/` |
| K8S-OQ-010 | RuntimeResource 已释放 lease 的 durable tombstone 如何做保留和压缩？ | PoC 为 stale Release/Prepare fencing 每个 Sandbox 保留一条释放记录；它不是 active lease 或运行资源，但生产版本需要有界保留 | 待指定 | S5 | OPEN | 定义保留窗口/压缩水位，验证节点重启与迟到 RPC 不会复活旧 generation，并做长稳磁盘增长测试 |
| K8S-OQ-011 | Kubernetes 可写 bind volume 如何在固定只读 virtio-fs share 中保持 `rw` 语义？ | 使用 Pod 级独立 `cubeVolumes` share：rootfs `cube.fs` 保持只读，Host Volume 通道可写，Guest 按标准 OCI mount 保持逐项 `ro/rw` | Codex | S3.1 | DECIDED | S3.1b `5b504b58` 已实现固定 Volume root 与 per-Task generation；`inv-a83rxbgg0g` 通过 137 项 Rust、Go race/vet 和 privileged bind cleanup；`inv-a83sdvgumu` 验证 disk/memory emptyDir 跨 init/app/sidecar 读写、同 source 的 `rw/ro`、只读 rootfs、Task/失败回滚清理和 tombstone +2，`inv-883sh80mpp` 独立审计通过 |
| K8S-OQ-012 | ephemeral container 的 `targetContainerName` 是否需要加入目标容器 PID namespace？ | 首版支持标准动态 ephemeral container、独立 PID namespace 与 `shareProcessNamespace`；不支持 per-target PID namespace join，并明确为运行时兼容性限制 | Codex | S5.3 | DECIDED | S2.4 `inv-983kt80r3f` 证明请求保留 `targetContainerName=app`，但 debugger 不会进入目标容器 PID namespace；最终 NodeConformance 的 common misc 分片 49/49 通过。首版不伪装 target join，调试目标进程时要求 Pod 使用 `shareProcessNamespace=true`；若后续需要精确 per-target join，作为独立增强重新立项 |
| K8S-OQ-013 | OCI `noNewPrivileges=true` 如何无损进入 Cube Guest？ | 移除 CubeShim protobuf 转换中的强制 `false`，保持 Agent 兼容并由 Guest 在启动进程前设置 NNP；不能静默降级 | Codex | S3.3d | DECIDED | `5a851245` 移除 NNP 强制清零并保持当前 RuntimeDefault seccomp 子集；对 protobuf 无法无损表示的 `defaultErrnoRet`、`listenerPath`、`listenerMetadata` 和显式 syscall `errnoRet=0` 在 Host Shim fail-closed。`inv-b8497x0qge` 完成最终 Shim 构建与全量测试；`inv-b849phgapi` 两轮验证 runc/Cube 的 NNP `false/true`、Guest `NoNewPrivs=0/1`、seccomp mode `0/2` 和 `unshare` 允许/阻断，lease `488→492` 且精确恢复基线；`inv-3849tjgnmx` 独立审计通过 |
| K8S-OQ-014 | 标准 Pod/container resources 如何分配到 Host Pod VM cgroup 与 Guest per-container cgroup，且不重复或遗漏约束？ | kubelet 独占 Pod parent；Cube leaf 只表达静态 VM capacity+RuntimeClass overhead；Guest resource parent 保留 OCI per-container 限制 | Codex | S3.4c | DECIDED | S3.4b 已关闭 Guest create/update；S3.4c 的 managed Sandbox 把 Shim/VMM/virtiofs 置于 OCI 指定 leaf。最终 `2a23aa3c` 上的 `inv-a86i6rgrkc`、`inv-686i7a0qux`、`inv-686i7a0quv`、`inv-386i7ags0q` 覆盖 BestEffort/Burstable/Guaranteed、classic init/restartable sidecar/app 与 Pod-level 的六 Pod、九容器三层 readback；原始逐对象证据及 SHA-256 随 handoff 保存。Host CPU/memory/PIDs、Guest OOM、survivor 和 exact baseline 通过。Cube 不重算 aggregate、不写 parent，以祖先/leaf 交集执行 |
| K8S-OQ-015 | `UpdateContainerResources` 与 Kubernetes in-place Pod resize 在 Cube VM 中如何同时更新 Host/Guest 两层限制？ | 不做跨 owner 伪事务：kubelet 更新 Pod parent，Task Update 更新 Guest 目标容器，Cube static leaf 不随 workload resize 重算 | Codex | S3.4d | DECIDED | containerd 2.3.4 不转发 sandbox Update，但 kubelet 先更新 parent。S3.4c 证明预留容量内 parent 自动更新、leaf 不变、Guest 精确更新；`inv-v86ksj0ndb` 又覆盖 containerd restart 后两次 resize、identity/restart/HTTP 稳定。超过静态容量转 K8S-OQ-018；持续 long-exec 并发限制转 K8S-OQ-022 |
| K8S-OQ-016 | CPU/memory 之外的 swap、PIDs、hugepages、ephemeral-storage 分别应由哪一层执行？ | swap/hugepage 与 workload PIDs 属于 Guest per-container；Host leaf 仅用独立 `pids.max` 保护 Shim/VMM threads；ephemeral-storage 由 kubelet 对 Host emptyDir/containerd snapshot 计量并驱逐 | Codex | S3.4d | DECIDED | S3.4b 已关闭 Guest swap/PIDs/hugepage 协议与压力边界；有限 Pod PID accounting 转 K8S-OQ-019。`inv-386mfegiev` 中 Cube Pod 写入 96Mi emptyDir、容器总限额 16Mi 后被 kubelet 标准判定 `Failed/Evicted`，Node `DiskPressure=False`；namespace 删除，`inv-086mip0u7c` W2 exact-zero。Guest 临时 rootfs upper 不回写 containerd，故首版 ephemeral-storage 支持面只声明 Host-backed emptyDir/标准 snapshot 可计量路径 |
| K8S-OQ-017 | checkBeforeUpdate=false 时，极端 `memory.max` 下调如何避免内核同步 reclaim 超过 Shim-Agent RPC deadline，并在未知完成状态下对账？ | Kubernetes owner 由 kubelet 在 CRI 前按当前用量保护；managed 直接 CRI 缺省为写前检查；只有显式 false 保留低层语义 | Codex | S3.4d | DECIDED | `5b4b15dc` 定向测试证明缺省 managed downsize 使用 check=true；`inv-a86ket058u` 证明 Kubernetes 1.36 在 CRI 前拒绝/延迟低于当前 usage 的下调且无 deadline wedge。首轮 `inv-986kd00166` 仅因错误要求 Agent 事件失败。显式 false 的低层同步 reclaim 风险不属于首版 Kubernetes 支持面 |
| K8S-OQ-018 | in-place resize 后 Pod aggregate 超过创建时静态 VM vCPU/memory 容量时如何处理？ | 首版只验收创建时通过 `cube.vmmres` 预留足够容量的 resize；不把容器 limit 更新等同于 VM hotplug | 待指定 | S6 | DEFERRED | 二期比较 VM CPU/memory hotplug、recreate/restore 与 containerd sandbox Update/status 扩展；在实现前，超过静态容量不声明支持 |
| K8S-OQ-019 | kubelet 配置有限 `podPidsLimit` 时，如何区分 Host VM threads 与 Guest workload PID 预算？ | PoC 使用 `podPidsLimit=-1`；Cube leaf 的 512 是 Host runtime safety ceiling，Guest OCI PIDs 是 workload 限制，二者不相减 | 待指定 | S5 | OPEN | 需要定义跨 VM 的 Pod PID accounting/API，验证 scheduler/kubelet eviction 与 Guest PID 汇总；完成前不支持有限 kubelet Pod PID 语义 |
| K8S-OQ-020 | RuntimeResource Release 遇到 systemd scope 已 inactive/消失时是否应视为幂等成功？ | scope 已无进程且 owner identity 与期望一致时按 already-gone 收敛；身份不确定仍 fail closed | Codex | S3.4c | DECIDED | `2a23aa3c` 仅在 owner identity 匹配、unit 确认 inactive/dead，且 cgroup 精确为空或已消失时幂等成功；active、populated、identity mismatch 和查询错误仍 fail closed。`4066b17d/6662316e` 覆盖非零退出但 stdout=`not-found` 与 1 秒内收集重读；`2dcf26b9/10b7af56` 在等待后、空 leaf 检查前后及 reset 前重验 identity，替代 leaf 定向测试随 `inv-v86nregacb` 通过。普通 Kubernetes 删除 `inv-b86i5egc5x` 无 `FailedKillPod`；超过重读窗口的创建即取消边界单列 `K8S-OQ-023` |
| K8S-OQ-021 | 默认小 VM 规格下并发启动多个启动即高内存负载的 Pod，`ReleasePending` 的容量边界和可诊断语义是什么？ | VM memory 至少 256 MiB、managed Host leaf 至少 512 MiB；更小显式请求 fail-fast，Guest workload limit 独立执行 | Codex | S3.4d | DECIDED | `7526fc29` 实现 floor；`inv-686k7g02nv` 默认规格 8/8 首次 `OOMKilled/137`；`inv-b86k9w0j42`/`inv-986k9vg064` 活动期 leaf=512MiB 且 Host oom=0；`inv-986k8v0uwi` 显式 64MiB 请求明确拒绝；删除与 exact-zero 通过 |
| K8S-OQ-022 | 长时间 exec 与 `UpdateContainerResources` 并发时，Agent 操作通道是否会阻塞并导致状态分叉？ | Shim RPC handle 不跨 await 加锁；Agent 同容器长操作彻底解串行延期到 S5.4，首版不声明支持持续长 exec+resize 组合 | Codex | S5.4 | DEFERRED | `011a05fa` 修复 Shim client mutex；`d40d9e07` 增加 clone-safe container gate 与 sandbox fence，确定性覆盖 start/update 与 delete/kill/pause。`inv-v86ksj0ndb` 的受控组合通过；激进压力 `inv-086kus0i8w` 仍首次 Agent Update/探针超时，但约 53 秒后收敛。`inv-086ncr074p` 的 exec+连续 resize+delete 在 0 秒删除且无 FailedKillPod。剩余 Agent 锁粒度作为 P1 已知限制转 S5.4 |
| K8S-OQ-023 | 多个 Sandbox 创建刚开始就同时取消时，systemd scope collection 如何避免瞬时 `FailedKillPod`？ | 保持精确身份与空 leaf fail-closed；kubelet 重试可收敛，首版允许可诊断的瞬时告警 | Codex | S5.4 | DEFERRED | `inv-086ncr074p` 中 12 Pod 创建 2 秒即删除全部成功但产生 3 个 scope collection `FailedKillPod`；`4066b17d/6662316e` 已覆盖明确 absent 与 1 秒内收集，`2dcf26b9/10b7af56` 保证等待期间 identity 变化时 fail-closed，但压力证明更长 collection 窗口仍存在。随后 `inv-a86ndt0pkt`/`inv-a86nds0c3q` 双 Worker exact-zero、`inv-v86nea09kv` 3/3 Ready。S5.4 评估异步 release/reaper 或延长/事件化等待并补 churn 零告警门禁 |
| K8S-OQ-024 | containerd 缺省 Cube handler 能否等价替代 Pod 的 `runtimeClassName: cube`？ | 不能；缺省 handler 只决定 CRI runtime，kubelet 必须从 Pod RuntimeClass 获得 overhead 和 scheduling | Codex | S5.3 | DECIDED | `inv-686q27g8sp` 中 15Mi Pod 的 ancestor Pod cgroup 只有 15Mi，宿主机 OOM 杀死 Shim/VMM；Cube leaf 无法放宽祖先限制。`inv-b86t69gf00` 注入 RuntimeClass 后 Pod 得到 256Mi overhead，`inv-386td0gr45` 的标准 NodeConformance OOM 门禁通过。正式架构必须显式 RuntimeClass；containerd 缺省 Cube 仅可用于不带资源限制的诊断，不是受支持部署方式 |
| K8S-OQ-025 | memory-backed EmptyDir 通过固定 virtiofs Volume 通道进入 Guest 后，如何保持 `tmpfs` mount identity？ | 当前数据、权限、FSGroup、跨容器共享和清理正确，但 Guest `statfs` 看到 FUSE；需要 Pod 级 Guest tmpfs 及可共享挂载生命周期 | Codex | S6.1 | DEFERRED | 最终 477 项归并中 `empty_dir.go` 的 12 项均只因期望 tmpfs、实际 FUSE magic 失败，其他 storage B 50/50 通过。不得伪造 statfs；S6.1 决定 Guest Pod 级 tmpfs、kubelet accounting、subPath 与快照交互，见 `evidence/s5.3/s5.3b-nodeconformance.md` |
| K8S-OQ-026 | NodeAllocatable Host OOM 用例在 VM runtime 上应如何解释？ | 不属于 `[NodeConformance]`；Guest 内存隔离不保证无容器 limit 时由 Host memcg 直接产生 exit 137 | Codex | S5.4 | DEFERRED | `inv-386td0gr45` 中 7 项定向测试 6 通过；唯一失败为无 NodeConformance 标签的 `OOMKiller for pod using more memory than node allocatable`，容器 exit 1 而非 137。先作为 VM runtime 语义差异记录，不阻断 S5.3 NodeConformance |
| K8S-OQ-027 | 官方 e2e_node 在 kubeadm 节点上如何执行 kubelet restart 测试？ | v1.36.4 helper 只匹配 `kubelet-\\w+`；测试期运行等价 `kubelet-e2e.service`，结束恢复原 unit | Codex | S5.3 | DECIDED | 初次门禁的 OOM `It` 已通过但 AfterEach 因 `kubelet.service` 名称失败；切换等价临时 unit 后 helper 正确识别 `kubelet-e2e`，标准 OOM 门禁通过。该变化只属于测试环境，不进入产品安装方案 |
| K8S-OQ-028 | privileged Guest 是否具备 Kubernetes 标准用例要求的 dummy netdev 能力？ | privileged 传递已生效；x86 BM/PVM Guest kernel 均应内建 `CONFIG_DUMMY=y`，避免依赖未打包模块 | Codex | S5.3 | DECIDED | `f9120d79` 统一 x86 BM/PVM Guest config（aarch64 原已内建）；`inv-b86w9i084p` 从最终 vmlinux 提取到 `CONFIG_DUMMY=y`，artifact SHA-256 `633bb982…`；`inv-v86wfwg2wn` 可回滚部署到 W1 后，官方 `PrivilegedPod` `inv-v86wga0nm0` 为 1/1 通过，`ip link add dummy1 type dummy` 成功，`inv-986wi0gicj` 最终全量归零 |
| K8S-OQ-029 | kubelet CPU Manager 分配的宿主机 CPU/NUMA ID 如何应用到只暴露 Guest-local 拓扑的 Cube？ | 在每个 Pod Sandbox 内保持稳定、无冲突的 host→Guest ID 映射；独占集合保持相等/分离关系，超出 VM 大小的共享池映射为全部 Guest vCPU，失败不提交映射 | Codex | S5.3 | DECIDED | `8dc39f77` 的 create/update 原子映射与失败回滚通过专项；最终 Device/PodResources 在 Agent `64381287` 上 10/10 通过、1 项仅因无 SR-IOV VF 跳过（`inv-389w3ggrk9`）。剩余 CPU Manager sidecar 失败不是 ID 映射，而是上游测试直接读取不存在的 Host per-container cgroup，转 `K8S-OQ-036` |
| K8S-OQ-030 | Host cgroup/lifecycle 的强一致校验如何避免把缓存镜像 Pod 热启动放大到约 6.5 秒？ | 独立 worker 进入 Pod leaf 时只做一次 D-Bus placement 强校验；后续使用 lease epoch、`/proc` membership 和 cgroup inode 轻量验证，不在正常启动路径执行 `systemctl show` | Codex | S5.4c | DECIDED | `fa278467` 将正常路径 `systemctl show` 从单 Pod 264 次降到 0；串行 50/50 的 Scheduled→Ready P95=1935.438ms、RunPodSandbox P95=1575.632ms，5×10 并发 50/50，PullImage=0；restart、worker/Shim kill、创建取消及 exact-zero 均通过。同一 reviewer `PASS`，证据见 `evidence/s5.4/s5.4c-startup-fastpath.md` |
| K8S-OQ-031 | 是否将内嵌 VMM 拆为每 Pod 一个 `cube-vmm-worker`，并由谁创建、放置、控制和回收？ | 每 Pod 一个独立 worker；CubeShim `fork/exec` gated worker、独占 Sandbox/Task 编排和 sandbox leaf/scope，Cubelet 只持 RuntimeResource lease，worker 不直接调用 Cubelet | Codex | S5.4a | DECIDED | 用户确认。fork 前持久化 generation/epoch/nonce spawn INTENT，worker 在 Hello/placement 前不创建 VMM，FD 使用 allowlist+SCM_RIGHTS。S5.4b 明确不做替代 Shim 重连：parent-death/控制 EOF 与 watchdog 精确回收后由 kubelet 重建；快照相关操作延后到 S6.1 扩展同一 IPC，不新增 worker↔Cubelet 控制面。完整契约见 `evidence/s5.4/s5.4a-worker-contract.md` |
| K8S-OQ-032 | “不含镜像拉取的 Pod 一秒启动”使用什么时间口径、分位数和负载？ | 主 SLO 为预拉取单容器无 probe Pod 的 PodScheduled→Ready P95≤1s；runtime 子预算为 RunPodSandbox 调用 P95≤700ms；50 次串行、5×10 并发、成功率 100%，P99/miss/fallback 单列 | Codex/用户 | S5.4a | DECIDED | 用户已确认 SLO。契约冻结同一观察进程 CLOCK_MONOTONIC、固定 busybox 负载、nearest-rank 算法、失败样本、CRI/Guest 子分段与无 PullImage 证据；普通 boot 和 RuntimeTemplate 命中/miss 分开统计。S5.4c E2E 入口门禁为 RunPodSandbox P95≤2s、create→Ready P95≤2.5s；同一 reviewer 第三轮 PASS，见 `evidence/s5.4/s5.4a-worker-contract.md` |
| K8S-OQ-033 | kubelet 管理的 `/etc/hostname` 如何在 Cube Guest/container 中呈现标准 Pod hostname 与 FQDN 语义？ | Kubernetes 标准依赖 OCI/UTS hostname 与 kubelet 管理的 `/etc/hosts`；首版不额外承诺 rootfs 中存在 `/etc/hostname` 文件 | Codex | S5.3b | DECIDED | 定向 hostname/FQDN/KubeletManagedEtcHosts 最终 6/6 通过，477 项归并中相关用例无失败。先前 `cat /etc/hostname` 不存在是文件呈现差异，不影响当前 Kubernetes 标准语义；若产品要求该文件，由 S6.1 与 snapshot 身份注入一起设计，不再作为 S5.3 阻断 |
| K8S-OQ-034 | virtiofs 导入 Guest 后如何保留普通只读卷内的嵌套 mount identity？ | 首版继续准确广告 `recursive_read_only=false`；普通 `ro` 可用，但嵌套 mount 不伪装为仍可见 | Codex | S6.1 | DEFERRED | 正确 shim manager privileged 门禁探针通过后，`inv-689xwbgpag` 中 mount init 成功、测试容器实际启动后 exit 1，排除了前置门禁；官方“不支持 RRO 时拒绝 recursive 请求”通过，两个支持分支按 capability 跳过。需要 Guest mount topology 重建或不同卷通道，见 S5.3 最终报告 |
| K8S-OQ-035 | static Pod 内 NFS server 如何再导出 virtiofs-backed EmptyDir？ | 当前 virtiofs 不是可被 Guest NFS server 稳定 re-export 的后端；首版不支持该组合 | Codex | S6.1 | DEFERRED | 正确 privileged 门禁下 NFS server 容器已 Ready；`inv-689xwbgpag` 中 Host kubelet 挂载 `PodIP:/` 时由 server 返回 `No such file or directory`，证明失败位于 Guest 内 virtiofs-backed EmptyDir 的 NFS export，而非容器启动门禁或网络不可达。候选方案是 Guest-local 可 export 文件系统、块设备或专用卷后端，必须同时定义快照与清理语义 |
| K8S-OQ-036 | 上游直接读取 Host per-container cgroup 的 CPU/PSI 用例如何适配“一 Pod 一 VM、容器 cgroup 在 Guest”的模型？ | 不创建伪 Host per-container cgroup；通过版本化 stats/topology 协议汇总 Guest per-container 数据，必要时提出上游 runtime-aware 测试接口 | Codex | S6.1 | DEFERRED | 最终仅 `summary_test.go:461` 的 I/O PSI 和 `cpu_manager_test.go:1909` 的 Host cpuset 文件读取失败；普通 CPU mapping、PodResources、CRI stats 与其他 summary 用例通过。S6.1 决定 worker/Agent stats 扩展是否与 snapshot IPC 一并演进 |
| K8S-OQ-037 | 挂接现有 kubeadm apiserver 时如何执行 kubelet client CA 热重载 NodeConformance？ | Cube 数据面不参与；应使用 e2e_node 自管 control-plane 或专用 apiserver fixture，避免让临时 kubelet CA 与既有 apiserver 信任分叉 | Codex | S6.1 | DEFERRED | `kubelet_server_tls_test.go:168` 最终 `/configz` 超时，是 477 项中唯一 runner/环境失败。当前集群恢复后继续使用原 kubelet/apiserver证书；未把控制面证书改动伪装成 Cube 修复，后续用专用 fixture 闭环 |
| K8S-OQ-038 | SR-IOV PodResources 标准用例如何完成硬件闭环？ | 需要给专用 PoC worker 配置至少一个 VF；无 VF 时官方测试主动 Skip，不能据此声明通过或失败 | Codex/用户 | S6.1 | DEFERRED | 最终 Device/PodResources 为 10 Passed、0 Failed、1 SR-IOV Skip，Ginkgo SHA-256 `60e50407…`。后续准备 VF 后复用同一 v1.36.4 binary 与分片，验证 VF allocation、CDI/OCI device、PodResources 和清理 |
| K8S-OQ-039 | 普通 OCI 冷启动 1.5 秒目标采用什么并发门禁，如何与最终一秒目标区分？ | S5.5 必需门禁为缓存镜像串行 PodScheduled→Ready P95≤1.5s、5×10 并发 P95≤1.8s 且相对串行增量≤300ms；冲刺串行和并发均≤1.5s；最终≤1s仍属于 S6.2/S5.4d RuntimeTemplate | Codex/用户 | S5.5f | DECIDED | 不含镜像拉取，固定 digest、`imagePullPolicy: Never`、50 串行、5×10 并发、成功率 100%，P99 和逐 Pod RunPodSandbox 单列。S5.5c 实测 network prepare 已达 4.313/6.293ms，但累计 CRI→start-vm 为 429.479/965.868ms；S5.5d.1 冻结的绝对 `CRI receive→Shim create begin` 负责≤80/150ms，扣除同 Pod CNI 的 Cube-controlled 数值只做内部归因；S5.5d.2 负责 durable create/cgroup（≤140/150ms），累计仍须≤220/300ms并重验 absolute start spread≤250ms；S5.5f 完整端到端门禁不变。不得以并发限流、轮均值或减少样本隐藏排队；完整预算见 `docs/zh/dev/kubernetes-runtime-performance-s5.5.md` |
| K8S-OQ-040 | Kubernetes 路径的真实单 Pod 资源开销和最终 `RuntimeClass.overhead` 应是多少？ | S5.5a 已冻结普通 cold 的 worker/embedded 与 1/5/20 Pod 基线；S5.5e 优化普通 cold 常驻页，RuntimeTemplate 的最终 CPU/memory overhead 只在 S6.2c 根据启动峰值、稳态 P99、压力与 100 Pod 密度标定 | Codex | S6.2c | OPEN | 最终制品 20 Pod×3 的 Host cgroup 为 worker 104.664MiB/Pod（95% CI 104.437～104.891）、embedded 99.989MiB/Pod（99.958～100.020）；worker/embedded 增量约 4.6MiB 和 3 Host PID。0/1/2 配对证明 virtiofs resident 增量仅为数百 KiB 量级，约 100MiB 主体是 Guest/VMM 私有常驻页，不能与历史模板 27～34MB 直接相减。S5.5e 普通路径目标为 20 Pod cgroup 均值≤100MiB 且节点边际下降≥15%或≤100MiB；S6.2c 模板命中 20 Pod 节点边际目标≤60MiB，并据实更新 RuntimeClass、安装清单和资源上限；证据见 `evidence/s5.5/s5.5a-final-baseline.md` |
| K8S-OQ-041 | watchdog 扫描生命周期进程时，目标在 PID 枚举后、读取 immutable identity 前退出应如何处理？ | 只有 `ENOENT/ESRCH` 且后续 durable owner/readback 证明同一进程已消失时按瞬时退出重试；其他 identity 错误继续 fail closed | Codex | S5.5d.2 | DEFERRED | 旧 S5.5d.1 的 10 Pod 创建中取消出现过 1 次 `stat identity /proc/<pid>/exe: ENOENT`，没有 `FailedKillPod` 且最终全部归零。修复版 `overlap-v2` 再次严格验证 scheduled=10/Pending=10，日志审计 panic=0、`FailedKillPod=0`，W2/W3 exact-zero（`inv-38dgxfgphd`、`inv-08dgxt0c0b`、`inv-a8dh0i04n8`、`inv-v8dh110p7p`、`inv-b8dh1eg0md`、`inv-98dh1egeix`），本轮未复现该告警；不能凭一次未复现关闭。S5.5d.2 仍需消除正常进程退出的 scanner error 日志，并补 PID reuse/identity mismatch 负向测试 |
